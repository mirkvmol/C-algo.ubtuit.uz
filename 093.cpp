    #include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x, y, a, b;
    cin >> x >> y >> a >> b;
    double s = 0, p = 1, p1 = 1, sp = 0;
    for (int a = 1; a <= x; a++) x{
        s += 1.0 * pow(a, 2) + 2 * a
    }
}
