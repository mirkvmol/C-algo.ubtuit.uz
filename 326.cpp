#include <bits/stdc++.h>
using namespace std;


int main() {
    int x, y, c, d;
    cin >> x >> y >> c >> d;
    double s = 0;
    for (int k = 1; k <= y; k++) {
        s+= (sqrt(c * 1 + d));
    }
    for (int k = 1; k <= y; k++) {
        p*= (sin(c + d) + 3 * c) / (cos(c * k) + 2.78 * d)
    }
    for (int i = 1; i <= c; i++) {
    for (int k = 1; k <= d; k++) {

    }
}
