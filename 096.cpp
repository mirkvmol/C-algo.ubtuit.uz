#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x, y, c, d;
    cin >> x >> y >> c >> d;
    double s = 0, p = 0, sp = 1, p1 = 0;

        for(int k = 1, k <= x; k++) {
            s+= pow((-1), k) * ((k + 1)) / (pow(k, 3) + pow(k, 2)+ 1);

        for(int k = 1, k <= y; i++) {
           p*= (pow(i = 1) );


        }
}
